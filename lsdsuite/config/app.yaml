service:
  name: "service_name"
  network:
    name: "network_name"
    subnet: "x.x.x.x/y"
  mem_limit: 314572800
  parameters:
    SOME: 500
    PARAMS: 1000
    FOR: 26
    YOUR: 4
    APP: 0
repository:
  name: "repository-with-trailing-slash/"
tracker: # Only add if you plan on using a tracker
  name: "tracker_name"
